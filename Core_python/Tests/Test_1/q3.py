# Km to meter & cm

kmDist = int(input("Enter distance in km : "))

mtrDist = kmDist * 1000
print(f'{kmDist} km into meter is : {mtrDist}')

cmDist = (kmDist * 1000) * 100
print(f'{kmDist} km into cm is : {cmDist}')